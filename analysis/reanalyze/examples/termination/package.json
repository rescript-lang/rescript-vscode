{
  "name": "termination",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "rescript build -w",
    "build": "rescript build",
    "clean": "rescript clean -with-deps",
    "analyze": "dune exe reanalyze.exe -- -config -ci -debug >src/termination.txt"
  },
  "devDependencies": {
    "rescript": "^9.1.4"
  }
}
